import React from 'react';
import styles from './Trail.module.scss';
import { TIME_FORMAT } from '../../constants';

const Trail = (searchTrail) => {
  // const elements = [];
  // let latestDay = null;
  
  // for (let query in props.queries) {
  //   if (showDates) {
  //     const day = moment(query.timestamp).format(TIME_FORMAT);

  //     if (day !== latestDay) {
  //       elements.push(<TrailDay day={day} />);
  //       latestDay = day;
  //     }
  //   }

  //   const queryImages = images.filter(i => i.queryId === query.id);
  //   elements.push(<TrailQuery query={query} images={images} />)
  // }
  
  // return (
  //   <div className={styles.Trail}>
  //     {elements}
  //   </div>
  // );

  return (
    <div></div>
  );
};

export default Trail;